---
title: ATP Agent
description: An agent that interacts with IQ's Agent Tokenization Platform
---

import { Tabs, TabItem, Aside } from '@astrojs/starlight/components';
import ExternalLink from "../../../components/ExternalLink.astro"

The ATP Agent is designed to interact with IQ's Agent Tokenization Platform. This agent provides a comprehensive set of features to manage tokens, including buying, selling, and managing tokens.

## Repository Location

This example is available in the <ExternalLink href="https://github.com/IQAIcom/brain-examples/tree/main/apps/atp-agent-example">brain-examples repository</ExternalLink>.

## Features

- Key feature 1
- Key feature 2
- Key feature 3

## Technologies Used

- **Plugins**: List of plugins used (with links to their docs)
- **Client Interface**: Which client interface is used
- **Model Provider**: Which AI model is used

## Code Highlights

<Tabs>
  <TabItem label="Agent Configuration">
    ```typescript
    // Show the key setup and configuration code
    const agent = new AgentBuilder()
      .withPlugin(...)
      .withClient(...)
      .build();
    ```
  </TabItem>

  <TabItem label="Plugin Integration">
    ```typescript
    // Show how plugins are initialized and configured
    ```
  </TabItem>

  <TabItem label="Core Functionality">
    ```typescript
    // Show the most interesting functional aspects
    ```
  </TabItem>
</Tabs>

## Setup Instructions

1. Clone the repository
2. Copy `.env.example` to `.env`
3. Configure the required environment variables:
   - `OPENAI_API_KEY`: Your OpenAI API key
   - Additional variables as needed

## Usage Examples

Show common interactions with the agent, for example:

